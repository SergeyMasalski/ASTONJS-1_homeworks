console.log(`
    "Неправильность" массивов:
    1. Длина массива должна быть неизменной.
    2. Тип данных в массиве должен быть одинаковым.

    Массивы представляют методы, которые характерны для других структур данных,
    например:
    - стек(методы push и pop), 
    - очередь(методы shift и unshift),
    - связанный список(методы find, forEach+splice, push, unshift)
`);
